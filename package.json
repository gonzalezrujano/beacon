{
  "name": "beacon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -F engine dev",
    "build": "pnpm -F engine build",
    "preview": "pnpm -F engine preview"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}